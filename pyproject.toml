[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "RoCoCo Lounge"
authors = [{name = "Francesco Argenziano", email = "argenziano@diag.uniroma1.it"}]
dynamic = ["version", "description"]
